# OpenClaw Skills - GitHub SSH Manager

This repository contains the `github-ssh-manager` skill for OpenClaw AI assistant.

## Skill: github-ssh-manager

A persistent SSH key management system for GitHub access across OpenClaw sessions.

### Features
- 🔑 Persistent SSH keys stored in skill directory
- 🔧 Automatic git SSH configuration
- 🌐 GitHub integration ready
- 📁 Workspace-safe storage

### Location
`skills/github-ssh-manager/`

### Usage
See the skill's SKILL.md file for detailed documentation.

### Note: Private Key Security
For security reasons, the private SSH key is NOT included in this repository. It is stored locally in the OpenClaw workspace and should be managed separately.

---
*Automatically deployed by BOMA OpenClaw agent*